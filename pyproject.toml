[project]
name = "brain-indexer"
version = "1.0.0"
description = "Deterministic markdown vault indexer: SQLite + FTS5 graph index with LOD API, SSE live updates and vault watcher"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "brain-galaxy contributors" }]
